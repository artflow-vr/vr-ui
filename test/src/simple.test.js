import HorizontalLayout from 'layout/horizontal-layout';

let tests = () => {

  it ( `test`, () => {
    //assert( true, `success` );
  } );

};

export default () => {

  describe( `Simple`, () => {

    tests();

  } );

};
