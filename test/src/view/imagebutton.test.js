let tests = () => {

    describe( `ImageButton`, () => {

        it( `instanciation`, () => {
        } );
        // TODO: Clone test
        /*it( `Recursive object`, () => {
            let result = {};
            setUndefinedProps( recObject, result );
            assert( compareObj( recObject, result ), `failed.` );
        } );*/

    } );
    //Element

};

describe( `View`, () => {

    tests();

} );
